<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingExtra="8sp"
        android:text="@string/new_employee"
        android:textColor="@color/colorMalachite"
        android:textSize="25dp"
        android:textStyle="normal" />

    <EditText
        android:id="@+id/edtEmployeeName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/employee_name_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:textColorHint="@color/colorGray"
        android:textSize="15dp" />

    <EditText
        android:id="@+id/edtEmployeeNickname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/employee_nickname_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:textColorHint="@color/colorGray"
        android:textSize="15dp" />
</LinearLayout>
